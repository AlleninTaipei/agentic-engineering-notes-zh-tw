# 讓 LLM 找到並使用開源函式庫: 文件入口, Markdown 與套件內建文件

> - 影片: [How We Got LLMs to Recommend Our Open Source Library](https://www.youtube.com/watch?v=V_5bn4q-vAI)
> - 頻道: [AI Engineer](https://www.youtube.com/@aiDotEngineer)
> - 講者: Christopher Burns, Inth 創辦人, c15t 開源 cookie consent banner 函式庫作者
> - 發布日期: 2026-08-26
> - 片長: 16:27
> - Video ID: `V_5bn4q-vAI`
> - 內容依據: YouTube 英文原語自動字幕 (`en-orig`), 影片公開 metadata 與章節
> - 整理語言: 繁體中文

## 核心觀點

Burns 分享的是改善自家函式庫 agent 使用體驗的實作經驗. 方法包括精簡文件入口, 提供 Markdown 版本, 以及將文件直接隨套件發布. 核心問題是讓 agent 在需要時取得相關內容, 減少搜尋與猜測 API 的成本.

影片標題提到讓 LLM 推薦函式庫, 但內容主要說明文件如何變得容易被 agent 讀取. 講者沒有提供能將特定文件改動與推薦量增加直接連結的對照實驗.

## 從開發者體驗到 agent 使用體驗

[02:23](https://www.youtube.com/watch?v=V_5bn4q-vAI&t=143s)

c15t 的 onboarding 表單會詢問使用者從何得知產品. Burns 表示, 從 4 月 13 日開始, 回答來自 Claude, ChatGPT 或 Gemini 等 LLM 推薦的人數出現增長, 並成為最大的 inbound 來源. 這是團隊依使用者回報得到的觀察.

過去安裝工具可能需要引導精靈或開發者親自協助, 現在團隊會交給使用者一段 prompt, 由 agent 接手安裝. 因此, 原本為開發者準備的清楚文件與安裝流程, 也成為 agent 成功使用產品的基礎.

## 多個文件入口共同降低查找成本

[04:20](https://www.youtube.com/watch?v=V_5bn4q-vAI&t=260s)

講者沒有把成果歸因於單一技巧. 團隊同時處理文件索引, sitemap, RSS 與 robots.txt 等入口, 並將共用工作抽成不綁定特定框架的文件產生流程. 輸入是 MDX 文件, 輸出則是供 agent 使用的相關檔案.

這個流程的價值在於把多項文件維護工作集中處理. 講者也明確表示, 它不是保證曝光的 SEO 工具.

### 精簡 llms.txt

[05:52](https://www.youtube.com/watch?v=V_5bn4q-vAI&t=352s)

文件有數百頁時, agent 首先需要知道去哪裡找答案. Burns 建議人工編排 `llms.txt`, 依 agent 可能提出的問題選擇重要入口. 他以約 40 行有用內容勝過 1,000 行雜訊描述團隊經驗, 這不是通用長度規格.

講者接著提到較完整的 LLM 文件入口, 將其描述為包含頁面連結與簡短用途說明的 sitemap. 筆記保留這個導航用途, 不把講者的命名與描述視為格式標準.

### 同一份內容提供多種 Markdown 取得方式

[07:07](https://www.youtube.com/watch?v=V_5bn4q-vAI&t=427s)

講者認為, agent 讀取 HTML 會承擔額外內容成本, 因此文件網站應提供 Markdown 版本. 他介紹三種入口:

| 入口 | 影片中的做法 | 用途 |
| --- | --- | --- |
| `.md` 網址 | 在文件頁面網址後提供 `.md` 版本 | 使用者可直接將 Markdown 連結交給 agent |
| HTTP 內容協商 | 請求標頭表示接受 Markdown 時, 回傳 Markdown | 讓同一網址依請求方式提供內容 |
| Query parameter | 使用 `mode=agent` 參數 | 讓無法設定請求標頭的 agent 也有取得方式 |

他也提到透過回應標頭告知存在替代的 Markdown 版本. 不過, 他無法確定所有 agents 是否支援或採用這個訊號. 提供入口與 agent 實際使用入口, 是兩件需要分別確認的事.

## 讓 agent 可以向文件查詢

[09:17](https://www.youtube.com/watch?v=V_5bn4q-vAI&t=557s)

除了下載頁面, 講者希望 agent 能直接向文件系統提出問題. 他以 Web MCP 描述仍在早期的方向, 並提到自家工具提供三類能力:

- `Search docs`: 搜尋文件.
- `Get pages`: 取得頁面.
- `Ask docs`: 向文件內容提問.

這段是講者的實作概述. 影片沒有充分交代協定細節, 瀏覽器支援或部署方式, 因此不能直接當成 WebMCP 的實作教學.

## 把文件放到 coding agent 已經在讀的位置

[10:09](https://www.youtube.com/watch?v=V_5bn4q-vAI&t=609s)

Burns 認為, 對函式庫作者而言, 最值得重視的是 coding agent 常直接讀取 repository 與 `node_modules`. 它可能根據過時的訓練知識與編譯後程式碼推測目前 API, 而沒有造訪文件網站.

團隊的做法是把 Markdown 文件隨套件發布, 再以 `AGENTS.md` 指向文件位置. agent 遇到問題時便能從已安裝套件中讀取說明. 講者也提到可用 skills 引導這個行為, 但團隊在沒有 skills 的情況下也觀察到效果.

這個安排把文件帶到實際工作環境. 對持續變更的函式庫, 講者還強調讀取隨套件提供的文件並核對版本, 以減少依舊知識推測的情況.

他表示, 在多個模型的測試中, 相較於上網搜尋與取得文件, 直接讀取套件文件可節省接近 50% tokens. 影片沒有提供完整測試題目, 模型版本, 樣本數或計量方式, 所以這個數字應視為團隊自述的結果.

## 檢查 agent 是否能取得內容, 並持續調整

[12:53](https://www.youtube.com/watch?v=V_5bn4q-vAI&t=773s)

講者提到網站 agent readiness 檢測工具, 用來找出可以改善的項目. 他展示的分數是 59, 並表示幾週前分數更高, 藉此說明檢測條件與生態仍在變化.

他的建議是逐步改善, 持續檢查. 工具評分可提供線索, 但影片沒有證明分數能預測 LLM 推薦量或產品採用率.

## 一般網站可以從哪裡開始?

[14:36](https://www.youtube.com/watch?v=V_5bn4q-vAI&t=876s)

問答中, 有聽眾詢問一般網站如何起步. Burns 建議準備 `llms.txt` 與較完整的文件入口, 並在可行時讓每個頁面都有 Markdown 版本. 如果 CMS 沒有這種能力, 仍可先人工整理入口文件, 再思考如何產生各頁內容.

這段回答延續整場分享的方向: 讓 agent 取得內容時更直接, 並減少讀取成本.

## 編輯整理: 可驗證的實作檢查

以下是依影片方法整理的檢查問題, 不是講者提供的實驗流程:

1. 給 agent 一個具體使用問題, 它能否從文件入口找到相關頁面?
2. Markdown 是否保留必要的 API 說明與範例, 並與網頁內容一致?
3. agent 的工具是否真的支援所提供的 `.md`, 標頭或 query parameter 入口?
4. 從發布後的套件檢查, 文件是否確實被包含, 並對應該套件版本?
5. 比較相同任務有無套件文件時的答案正確性與 token 用量, 能否重現改善?

其中, 文件可取得性, 使用正確性與推薦來源應分開觀察. 文件更好讀不等於推薦量一定增加.

## 來源與限制

筆記依完整英文自動字幕重新整理, 時間戳採影片公開章節. 未逐格核對投影片, 也未實際測試網站標頭, 工具或套件內容. 自動字幕對部分工具名稱與成長數據有辨識歧義, 因此省略不必要的數字與未確認拼字.

講者是 c15t 作者與實作者, 分享具有第一手經驗基礎, 同時也涉及自家產品與工具. 推薦來源增長與 token 節省均為講者自述, 缺乏完整公開實驗細節. 文中技術做法是影片當時的經驗紀錄, 不代表所有 agents 的共同能力或現行標準.
