# Anthropic Claude Prompt Workshop: 從失敗案例迭代提示詞

> [!info] 來源
> - 影片: [Building with Anthropic Claude: Prompt Workshop with Zack Witten](https://www.youtube.com/watch?v=hkhDdcM5V94)
> - 頻道: AI Engineer
> - 發布日期: 2024-08-17
> - 片長: 01:34:56
> - Video ID: `hkhDdcM5V94`
> - 字幕來源: YouTube 英文自動字幕 (`en-orig`)
> - 整理語言: 繁體中文

## 一句話總結

有效的提示詞工程不是堆疊咒語, 而是用真實失敗案例反覆測試, 清楚分隔資料與指令, 提供高品質範例, 並把格式驗證、路由與停止條件等確定性工作交給程式。

## 工作坊的核心方法

這場工作坊採用「prompt doctor」形式。參與者帶來正在使用的提示詞與失敗案例, 講者 Zack Witten 在 Anthropic Console 中直接修改、執行並比較結果。

一個實用的迭代循環是:

1. 先保留目前的提示詞作為基準。
2. 蒐集多個真實輸入, 尤其是會失敗的邊界案例。
3. 每次只改一個主要因素, 例如指令位置、輸出格式或範例。
4. 在相同測試集上比較結果, 不以單次成功判斷品質。
5. 能以程式驗證的條件, 直接寫驗證器, 不再呼叫模型判斷。

影片所示的 Console Evaluate 功能可並排執行多個測試案例。這提醒我們, 提示詞應被視為需要回歸測試的程式資產, 而不是一次寫完的文字。

## 先把提示詞的結構整理清楚

### 用有語意的區塊分隔內容

講者建議用 XML 標籤分隔文件、使用者訊息、範例與輸出區塊。重點不只是模型可能熟悉 XML, 更重要的是讓每一段文字的角色明確。

```xml
<document>
{{DOCUMENT}}
</document>

<instructions>
根據文件回答問題。
回答 1 至 2 句, 最多不得超過 3 句。
</instructions>
```

變數名稱也應表達用途。與其把不同來源全部塞進模糊的 `{{text}}`, 不如區分 `{{DOCUMENT}}`、`{{USER_MESSAGE}}` 與 `{{PROJECT_PATH}}`。

### 長文件在前, 最終指令靠近結尾

對長篇上下文, 影片建議先放文件與資料, 再於靠近提示詞結尾處放置要模型執行的指令 ([00:42:36](https://www.youtube.com/watch?v=hkhDdcM5V94&t=2556s))。講者認為靠近結尾的指令往往被遵循得更緊密, 但仍強調應用自己的測試集驗證。

### 把模糊要求改成可檢查的限制

「請簡潔」沒有共同尺度。若產品需要短答, 可直接寫成「回答 1 至 2 句, 絕不超過 3 句」。明確限制能降低模型自行解釋規格的空間, 也方便後續自動驗證。

影片亦提到修正大小寫、拼字與文法。這不保證一定提高品質, 但清楚、簡單的句子通常比混亂或過度學術化的措辭更可靠。

## 結構化輸出: 提示詞只負責一部分

### Assistant prefill

JSON 案例中, 模型常在資料前加上「以下是 JSON」等前言。講者展示 Claude API 的 assistant prefill: 在最後一則 assistant 訊息中預先填入開頭, 讓模型從指定位置續寫 ([00:27:01](https://www.youtube.com/watch?v=hkhDdcM5V94&t=1621s))。

例如預填 `{` 能把生成導向 JSON, 但呼叫端必須把被預填的字元補回, 才能交給 JSON parser。這是介面層控制, 不是在一般使用者提示詞中多寫一句 `{` 就能完全取代。

### 標籤、解析與 stop sequences

另一種做法是要求模型把結果放入 `<json>...</json>` 或 `<summary>...</summary>`, 呼叫端只擷取標籤內內容。API 還可把關閉標籤設為 stop sequence, 讓生成在指定位置停止 ([00:33:16](https://www.youtube.com/watch?v=hkhDdcM5V94&t=1996s))。

由此可得到一條重要原則:

> 能以 parser、正規表示式、schema validator 或停止條件完成的工作, 優先交給程式。不要為可確定處理的事情增加一大段不穩定提示詞。

同理, 要檢查輸出是不是合法 JSON, 應直接解析 JSON。只有「摘要是否忠實」或「語氣是否自然」這類難以寫成規則的品質判斷, 才值得考慮人工或模型評分。

## Few-shot 範例往往比措辭微調更重要

社群貼文案例要求模型「不要尷尬、不要浮誇, 但要有吸引力」。這些抽象形容詞很難僅靠定義達成。講者認為, 多數提示詞成效來自挑選與編修高品質範例, 其重要性可能高於其他措辭技巧 ([00:51:23](https://www.youtube.com/watch?v=hkhDdcM5V94&t=3083s))。

可採用以下流程:

1. 讓模型為一份文件產生大量候選結果。
2. 人工挑選少數最好者並編修到可接受的標準。
3. 將「原始文件 + 理想輸出」放入範例區。
4. 隨著新失敗案例出現, 逐步建立範例集。

如果 token 預算有限, 講者傾向先放一個非常好的完整範例, 而不是多個品質較差或被截斷的範例。不過單一範例可能讓模型過度模仿其偶然特徵, 因此仍要用實際輸出驗證。

### 正例、反例與對照組

反例是工具, 不是每個提示詞的必備元素。對「尷尬文案」這類難以定義的問題, 可並排提供:

- 同一份輸入的差勁輸出, 並說明問題在哪裡。
- 同一份輸入的理想輸出, 並說明它符合哪些要求。

這種對照比單純寫「不要尷尬」更具體 ([00:53:44](https://www.youtube.com/watch?v=hkhDdcM5V94&t=3224s))。負面指令則應輕量使用, 避免反覆強調禁止項目, 反而讓模型持續關注它。

### 先規劃, 再輸出

影片建議在範例中示範「先列出文件重點, 再撰寫貼文」。這種先分析再作答的結構可讓模型先建立選材依據。實務上可要求簡短、可檢查的中間分析, 不必把冗長的內部推理當成產品輸出。

## 角色扮演與多 Persona

單一提示詞同時模擬多個 Persona 時, 講者更傾向為每個 Persona 建立獨立提示詞, 再由程式依使用者選擇路由到對應設定 ([01:00:47](https://www.youtube.com/watch?v=hkhDdcM5V94&t=3647s))。這比要求同一模型在一大段提示詞內切換多種身分更容易管理。

若只能在單一聊天中運作, 可要求每則回覆以目前 Persona 名稱開頭, 並提供「維持角色, 不必解釋自己的 Persona」之類的輕量約束。

影片中的個人經驗是, system prompt 主要放角色, 一般操作指令放 human/user prompt。但現場也有人提出不同觀察。這不是普遍定律, 應依所用模型、API 版本與測試資料決定。

## 多語提示詞

若目標輸出不是英文, 理想作法是請熟悉目標語言的人直接撰寫或審校提示詞。若作者只熟悉英文, 一份清楚的英文提示詞可能比生硬、錯誤的目標語言提示詞更有效。語言選擇不是越本地化越好, 關鍵是規格是否準確。

## 圖像與 OCR 任務

手寫資料辨識案例帶出三項實務建議:

1. 裁切到真正需要辨識的區域, 移除無關視覺資訊。
2. 提供可取得的最高品質影像, 必要時放大局部。
3. 先要求模型描述它看見的全部內容, 再進行擷取或計算。

需要注意, 模型若一開始把某個字元看錯, 可能為了維持自洽而把相同錯誤延續到其他區域。對關鍵數字仍需人工複核, 或使用專門 OCR 與規則檢查。

## 使用模型評分時, 先處理評分偏差

翻譯評分案例要求模型用 1 至 5 分評價英文到日文的翻譯, 但模型會把文章主題是否正面、內容是否合宜, 混同為翻譯品質。提示詞應明確要求分開判斷內容性質與語言品質 ([01:24:02](https://www.youtube.com/watch?v=hkhDdcM5V94&t=5042s))。

改善方式包括:

- 為 1 至 5 的每一級至少提供一個已標註範例。
- 說明每個範例為何屬於該級, 並列出評分依據。
- 先要求逐項分析文法、準確度與流暢度, 最後才輸出分數。
- 避免 1 至 100 這類過細尺度。模型未必能穩定校準, 約五個類別通常較合理。
- 用人工標註集檢查評分器的一致性與偏差, 不因模型能解釋理由就假設分數正確。

若只是驗證格式, 不應使用模型評分。若品質本身帶有主觀性, 模型評分可以作為輔助, 但仍需要校準資料與人工抽查。

## 降低摘要幻覺: 先找證據, 再摘要

影片末段提出一個重要做法: 先要求模型擷取與問題相關的原文片段, 再要求它只根據這些片段撰寫摘要 ([01:32:39](https://www.youtube.com/watch?v=hkhDdcM5V94&t=5559s))。

```xml
<relevant_quotes>
先列出文件中與問題直接相關的片段。
</relevant_quotes>

<summary>
只根據上方片段撰寫摘要。
</summary>
```

這種「證據先行」結構讓讀者能追查摘要依據, 也縮小模型自由補充的空間。不過擷取的引文同樣可能出錯, 高風險場景仍應比對原文。

## 可直接採用的提示詞檢查表

- 資料、使用者輸入、範例與指令是否有清楚邊界?
- 最終任務是否靠近提示詞結尾?
- 「簡短、自然、高品質」等形容詞能否改成可觀察規格?
- 是否有足夠的真實失敗案例作為測試集?
- 是否提供至少一個經人工編修的高品質範例?
- 難以定義的風格問題是否需要正反對照範例?
- JSON、標籤、長度與停止條件能否交給程式處理?
- 長任務能否拆成較小步驟或較少欄位?
- 摘要與問答能否先擷取證據, 再生成結論?
- 模型評分是否有各級範例、校準集與人工抽查?

## 時效性與使用限制

本片發布於 2024 年 8 月, 示範的是當時的 Claude 3 與 Claude 3.5 Sonnet、Anthropic Console 和 API 行為。模型能力、system prompt 遵循程度、結構化輸出功能、prefill 支援及 API 參數都可能已改變。

因此, 這份筆記最值得保留的是方法論: 清楚分區、明確規格、高品質範例、測試驅動迭代, 以及以程式承擔確定性工作。涉及特定產品功能時, 應再查閱目前版本的官方文件。

## 結語

工作坊反覆回到同一個觀點: 提示詞工程是一套實驗與系統設計工作。好提示詞不一定最長, 而是能清楚界定輸入與任務, 用範例表達隱性標準, 並在程式、測試與人工審查的配合下穩定產生可用結果。
