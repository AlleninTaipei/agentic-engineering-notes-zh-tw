# Web Automation 的進階方法: 讓代理像人類一樣操作網站

> 影片: [The Dark Arts of Web Automation: Teaching Agents to Use Websites Like Humans — Corey Gallon, Rexmore](https://www.youtube.com/watch?v=26RtyAm9y_Q), AI Engineer<br>
> 頻道: AI Engineer<br>
> 上傳日期: 2026-08-14<br>
> 片長: 21:37<br>
> Canonical URL: https://www.youtube.com/watch?v=26RtyAm9y_Q<br>
> Video ID: `26RtyAm9y_Q`<br>
> 內容依據: 英文原始自動字幕

## 摘要

這場演講介紹一套瀏覽器代理的方法論: 透過 CLI 與 Chrome DevTools Protocol (CDP) 操作瀏覽器, 以 `sense -> act -> verify` 迴圈逐步完成任務, 並只在網站要求時提高互動的擬人程度. 探索成功後, 再把有效路徑保存為確定性程式或 agent skill, 避免模型每次重新推理整套操作.

講者的核心工程觀點是將工作拆成兩部分. 可預測, 可重播的瀏覽器操作交給程式碼, 只有需要視覺理解或語意判斷的環節才呼叫模型. 這能降低模型往返次數, token 成本與延遲, 並提高限時流程的成功率.

影片也示範了反自動化與 CAPTCHA 場景. 這些機制是網站的安全和濫用防線, 未經授權規避可能違反服務條款或法律. 本筆記保留架構, 觀測與驗證方法, 不將示範改寫成可直接繞過第三方防護的操作指南.

## 1. 問題: 網站並不總是接受代理輸入

[00:00](https://www.youtube.com/watch?v=26RtyAm9y_Q&t=0s)

講者希望代理能代替人類預訂服務, 寄送郵件與填寫表單. 困難在於網站一旦判斷輸入不像真人操作, 可能忽略事件, 顯示額外驗證, 或直接阻止流程.

準備演講期間, 講者因相關瀏覽器實驗收到 OpenAI 的 cyber abuse 帳號警告. 他表示後來經溝通撤銷警告, 並強調現場展示只在自己擁有的基礎設施與帳號上進行.

這段開場同時揭示技術界線: 能在瀏覽器中完成某件事, 不代表在第三方系統上有權執行.

## 2. 核心前提: CDP 輸入沿著瀏覽器的原生路徑前進

[01:50](https://www.youtube.com/watch?v=26RtyAm9y_Q&t=110s)

講者以一句刻意誇張的比喻概括主張: 由 CDP 驅動的瀏覽器, 對網站而言可以很像一個拿著滑鼠的人.

Chrome DevTools Protocol 是瀏覽器開發工具背後使用的協定. 代理能透過它讀取頁面狀態, 監看網路與 console, 發送鍵盤或指標輸入, 並控制導覽.

不過, 「無法區分」應理解為講者對特定輸入路徑的工程描述, 不是對所有風險偵測系統的普遍保證. 網站仍可結合行為模式, 帳號狀態, 網路訊號與伺服器端規則偵測自動化.

## 3. 為什麼選擇 CLI 而不是 MCP

[02:43](https://www.youtube.com/watch?v=26RtyAm9y_Q&t=163s)

影片並未主張 CLI 在所有面向都比 MCP 更有能力. 講者的重點是, 對高頻率瀏覽器操作而言, CLI 比每一步都經過模型的工具迴圈更容易重用, 更快且更省 token.

| 面向 | CLI 程式 | 每步經模型的工具流程 |
| --- | --- | --- |
| 重用 | 成功序列可寫成程式並反覆執行 | 每輪通常仍需模型參與 |
| 延遲 | 確定性步驟在本機連續執行 | 多次模型 round trip 增加等待 |
| 成本 | 只在需要判斷時消耗模型 token | 工具選擇與回報持續占用 context |
| 適用工作 | 已知且穩定的互動序列 | 未知情境, 探索與語意判斷 |

講者引用一項研究, 聲稱 CLI 與 MCP 的任務成功率都約為 83%, 但 MCP 版本用了 71 次往返與約 8 分鐘, CLI 則約 7 turns 且不到 1 分鐘. 他也轉述 Anthropic 的資料, 稱 CLI 的 token 成本可能低很多. 本筆記未取得研究名稱的可靠字幕拼寫, 也未獨立核對這些數字, 因此應視為影片中的引用, 不是已驗證的普遍 benchmark.

## 4. CDP 提供代理的數位感官

[04:06](https://www.youtube.com/watch?v=26RtyAm9y_Q&t=246s)

CDP 的介面很大, 而且持續變化. 實際瀏覽器代理不必使用全部 domain, 可以按照感知與操作需求選擇小型子集:

### 看見頁面

- DOM: 讀取文件結構與元素狀態.
- Accessibility tree: 讀取元件語意與可操作角色.
- Screenshot: 取得像素資訊, 處理只能以視覺判斷的狀況.

### 聽見頁面

- Network traffic: 確認請求, 回應與背景資料交換.
- Console 與 logs: 發現錯誤及頁面內部訊號.

### 操作頁面

- Pointer 與 keyboard input.
- Navigation.
- 表單輸入與選取.

這種分類比背誦所有 CDP method 更適合設計 agent tools. 工具應讓代理清楚知道自己能感知什麼, 執行什麼, 以及如何取得外部證據.

## 5. Sense, act, verify 迴圈

[05:47](https://www.youtube.com/watch?v=26RtyAm9y_Q&t=347s)

代理每次只採取一個動作, 並用不同訊號驗證結果:

```text
Sense
  -> 確認目前頁面與目標狀態
Act
  -> 執行一個 click, type, select 或 navigation
Verify
  -> 從畫面, DOM, network 或其他通道確認效果
  -> 未完成則重新感知
```

關鍵規則是不要只相信工具自己的成功回報. Click API 回傳成功, 只表示輸入已送出, 不表示網站狀態真的改變. 比較可靠的驗證包括:

- 按下按鈕後查看頁面是否出現預期狀態.
- 送出表單後檢查 network response.
- 導覽後檢查 URL 與主要內容.
- 執行操作後重新讀取 accessibility tree.

這是可泛化到各種 agent tool use 的原則: 動作與驗證應盡可能來自不同觀測通道.

## 6. 三層互動階梯

[06:38](https://www.youtube.com/watch?v=26RtyAm9y_Q&t=398s)

當迴圈無法完成, 代表頁面開始抵抗目前的操作方式. 講者提出三層階梯, 原則是從成本最低的方法開始, 只爬到足以完成合法任務的層級.

### 第一層: 頁面內的簡單程式操作

若網站接受 DOM 或 JavaScript 觸發的互動, 就使用最簡單, 快速且容易批次化的方法. 它適合受控應用, 內部工具或對自動化友善的頁面.

### 第二層: 瀏覽器輸入事件

有些頁面會區分合成事件與瀏覽器輸入路徑產生的事件. 代理可以改用 CDP 的 input 能力, 使事件經由 Chrome 的正常輸入處理流程送達頁面.

### 第三層: 視覺與行為層

當任務需要理解畫面, 空間位置或連續互動時, 代理才加入 screenshot, vision 與較完整的指標軌跡.

編者提醒: 第二與第三層也可能被用來規避網站的反濫用設計. 實務上應限制於自有系統, 明確授權的測試環境, 或允許自動化的服務, 並遵守 API 與網站政策.

## 7. 先探索, 再把有效路徑固化

講者將工作分成兩個階段:

1. Explore: 人與代理逐步執行 sense, act, verify, 找出實際有效的互動方式.
2. Automate: 將成功路徑寫成程式, agent skill, 或兩者的組合.

如果每次執行都讓模型重新探索, 系統會變慢, 昂貴且不穩定. 固化後, 模型只負責輸入內容或處理真正有變化的部分.

這也形成一種知識累積機制:

```text
未知網站流程
  -> 代理探索與驗證
  -> 找到可靠操作序列
  -> 編碼為 CLI 程式
  -> 包裝為 skill
  -> 後續任務直接重用
```

## 8. 批次郵件案例: Web UI 作為可用介面

[08:27](https://www.youtube.com/watch?v=26RtyAm9y_Q&t=507s)

第一個案例從 Outlook Web 寄送多封個人化郵件. 頁面接受簡單操作, 因此程式負責開啟撰寫視窗, 填入欄位並送出, 模型只改寫每封郵件的個人化內容.

講者解釋, 企業的 Office 365 API 可能需要 app registration 與管理員核准, 但員工已具有 Web 登入權限. 因此, Web UI 有時可成為現有權限範圍內的操作介面.

不過, 影片使用的 "permissionless API" 是修辭性說法, 不代表可以繞過組織政策. UI 登入權限也不必然包含大量自動操作, 批次寄信還涉及速率限制, 垃圾郵件政策與個資處理規範.

## 9. 事件可信度與獨立驗證

[10:44](https://www.youtube.com/watch?v=26RtyAm9y_Q&t=644s)

影片以購物頁面說明, JavaScript 合成 click 可能被頁面忽略, 而經瀏覽器 input pipeline 的事件才會生效. Chrome 事件物件中的 trusted 與 untrusted 訊號可以成為頁面判斷來源之一.

這個案例再次說明為何 `act` 後必須 `verify`. 如果只檢查執行工具是否報錯, 無法發現頁面靜默丟棄輸入. 代理要重新觀察購物車狀態或網路活動, 才知道動作是否真正生效.

## 10. 反自動化示範的工程觀察

[12:51](https://www.youtube.com/watch?v=26RtyAm9y_Q&t=771s)

影片先展示 Cloudflare Turnstile, 接著在 [14:26](https://www.youtube.com/watch?v=26RtyAm9y_Q&t=866s) 展示圖像文字挑戰與拖曳拼圖, 最後處理 reCAPTCHA v2. 這些示範的共同工程觀察包括:

- DOM 不一定能直接存取所有可見元件, 例如跨來源 iframe 或封閉的 shadow root.
- 視覺位置與 DOM 結構是兩種不同的頁面表達.
- 某些驗證不只檢查答案, 也可能觀察互動時間與行為軌跡.
- 多輪挑戰可能過期, 因此模型 round trip 延遲會直接影響結果.

本筆記不記錄針對特定 CAPTCHA 的具體規避步驟. 對防禦方而言, 這些案例提醒我們, 單靠前端元素隱藏, event trust 或類人滑鼠軌跡不能構成完整的信任證明. 高風險操作還需要伺服器端授權, 速率控制, 異常偵測與交易層確認.

## 11. Solver 與 operator 的分工

[16:36](https://www.youtube.com/watch?v=26RtyAm9y_Q&t=996s)

最後的示範將系統拆成兩個角色:

| 角色 | 實作方式 | 負責內容 |
| --- | --- | --- |
| Solver | 確定性程式碼 | 驅動流程, 擷取狀態, 處理重試與時間限制 |
| Operator | 視覺模型或 agent | 只處理程式無法可靠判斷的圖像語意 |

程式不需要模型來決定每次 click. 它以機器速度執行已知流程, 只在需要辨識圖片時詢問一次模型. 講者認為這種分工是限時多輪任務能完成的關鍵.

這項模式可以安全地泛化到其他領域:

- 程式控制固定資料管線, 模型判讀例外文件.
- 測試框架執行所有步驟, 模型分析失敗畫面.
- 工作流引擎處理狀態轉移, 模型只分類非結構化輸入.
- 瀏覽器程式執行核准過的流程, 模型處理頁面文案變化.

## 12. 方法論才是重點

[19:47](https://www.youtube.com/watch?v=26RtyAm9y_Q&t=1187s)

講者希望觀眾帶走的不是個別 CAPTCHA 技巧, 而是以下方法:

1. 提供能被程式化與重用的 CLI.
2. 透過 CDP 取得多種頁面感知與操作能力.
3. 執行 `sense -> act -> verify` 閉環.
4. 從最低成本的方法開始, 必要時才提高互動複雜度.
5. 探索成功後, 立即把路徑保存為程式或 skill.
6. 讓確定性程式負責流程, 模型只處理需要感知和推理的環節.

代理能力不是只由模型決定. 講者的成果來自工具介面, 回饋迴圈, 程式化重用與速度最佳化共同形成的 harness.

## 安全且可持續的應用原則

編者整理, 根據影片方法加入適合正式環境的邊界:

- 優先使用官方 API. 只有在 API 不存在且政策允許時才考慮 UI automation.
- 僅操作自己擁有, 明確授權或專為測試建立的帳號與環境.
- 不把 CAPTCHA 繞過視為一般產品功能. 它通常代表服務方正在要求人工驗證或阻止自動化.
- 為寄信, 購買, 發布與刪除等外部效果設置人工核准或交易上限.
- 保存每個代理動作與驗證證據, 但避免在 log 中暴露密碼, token 或個資.
- 對頁面更新, selector 失效, 重複提交與部分成功設計復原機制.
- 將探索權限與正式批次執行權限分開.

## 來源與限制

- 本筆記依英文原始自動字幕與影片正式章節整理, 不是逐字稿.
- 自動字幕將講者反覆使用的比喻辨識為近似 `meat bag ladder`. 本筆記以「三層互動階梯」表達其工程含義, 避免依賴可能有誤的專有命名.
- 影片提到 CLI, MCP 的成功率, 執行時間與 token 成本數字, 但字幕不足以可靠識別完整研究來源. 這些數據未經本筆記獨立驗證.
- 影片中的 CAPTCHA 成功率與可重複性是講者陳述. 反自動化系統會持續改變, 不應視為目前仍成立的普遍產品能力.
- CAPTCHA 與反機器人機制具有安全, 法律與服務條款風險. 本筆記刻意不提供可直接套用於第三方網站的規避程序.
